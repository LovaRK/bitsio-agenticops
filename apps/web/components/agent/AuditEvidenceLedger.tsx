"use client";
import type { AuditEntry } from '@/types/agent';
export function AuditEvidenceLedger({entries,onOpen}:{entries:AuditEntry[];onOpen:(id:string)=>void}){return <section className="rounded-xl border border-white/10 bg-white/5 p-4"><h3 className="text-sm font-semibold text-white">Audit Evidence Ledger</h3><div className="mt-2 space-y-2">{entries.map(e=><button key={e.id} onClick={()=>onOpen(e.id)} className="block w-full rounded-lg border border-white/10 bg-slate-900/40 p-3 text-left"><p className="text-xs text-white/60">{new Date(e.timestamp).toLocaleString()} · {e.humanApproval??'not_required'} · Audit ID {e.id}</p><p className="text-sm text-white">{e.decision}</p><p className="text-xs text-white/65">Workspace {e.workspaceId} · Evidence {e.evidenceIds.length} · Confidence {Math.round(e.confidence*100)}%</p></button>)}</div></section>;}

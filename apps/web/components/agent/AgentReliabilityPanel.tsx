"use client";
import type { AgentReliabilityMetrics } from '@/types/agent';
export function AgentReliabilityPanel({m}:{m:AgentReliabilityMetrics}){return <section className="rounded-xl border border-white/10 bg-white/5 p-4"><h3 className="text-sm font-semibold text-white">Agent Reliability</h3><div className="mt-3 grid grid-cols-2 gap-3 md:grid-cols-4 text-sm text-white/80"><div>Latency {m.decisionLatencyMs.toFixed(0)}ms</div><div>Success {(m.decisionSuccessRate*100).toFixed(1)}%</div><div>Evidence {(m.evidenceCompleteness*100).toFixed(1)}%</div><div>Error {(m.errorRate*100).toFixed(1)}%</div><div>Live Ratio {(m.liveSplunkRatio*100).toFixed(1)}%</div><div>Fallback Usage {(m.fallbackUsageRate*100).toFixed(1)}%</div><div>Queue Aging {m.approvalQueueAgingHours.toFixed(1)}h</div><div>Trend points {m.confidenceTrend.length}</div></div></section>;}
